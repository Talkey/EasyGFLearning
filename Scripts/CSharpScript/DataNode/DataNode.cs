using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataNode : MonoBehaviour
{
    Rigidbody Rig;
    // Start is called before the first frame update
    void Start()
    {
        //Rig.angularVelocity.z
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
